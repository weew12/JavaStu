package site.weew12.chapter5;

import java.util.Arrays;

/**
 * 数组常见的算法
 * @author weew12
 */
public class AlgorithmOfArray {

    public static void main(String[] args) {

        /*
        数组求：总和、均值
        指定范围[a, b] : int num = (int)Math.random() * (b - a + 1) + a
         */
//        int[] arr = new int[10];
//        for (int i = 0; i < arr.length; i++) {
//            arr[i] = ((int) (Math.random() * (100)) + 1);
//        }
//        System.out.println(Arrays.toString(arr));
//        int sum = 0;
//        for (int i : arr) {
//            sum += i;
//        }
//        System.out.println("sum of arr:" + sum);
//        System.out.println("average of arr:" + ((double) sum) / arr.length);


    }
}
